kind: Pod
apiVersion: v1
metadata:
  name: "goserver"
  labels:
    app: "goserver"
spec:
  containers:
    - name: "goserver"
      image: "brendosp/hello-go:latest"
      ports:
        - containerPort: 8080
